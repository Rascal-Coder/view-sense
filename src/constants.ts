const interval = 100;
const triggers: ('scroll' | 'resize' | 'load')[] = ['scroll', 'resize', 'load'];
const dimensions: ('top' | 'right' | 'bottom' | 'left')[] = [
  'top',
  'right',
  'bottom',
  'left',
];
export { interval, triggers, dimensions };
